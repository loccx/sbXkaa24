{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase": "^10.7.1",
    "localtunnel": "^2.0.2",
    "morgan": "^1.10.0",
    "node-fetch": "^2.7.0",
    "openai": "^4.24.7",
    "puppeteer-core": "^21.7.0"
  },
  "scripts": {
    "start": "(node server.js & npx localtunnel --port 3000) && sudo kill -9 $(sudo lsof -t -i:3000)",
    "dev": "nodemon server.js"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
